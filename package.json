{
  "name": "perfumes-monolith",
  "private": true,
  "workspaces": ["perfumes-api", "perfumes-web"],
  "scripts": {
    "build": "cd perfumes-web && npm install && npm run build",
    "start": "cd perfumes-api && npm install && node src/index.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "react-router-dom": "^7.6.3"
  }
}
